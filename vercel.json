{
  "version": 2,
  "builds": [
    {
      "src": "nuxt.config.mjs",
      "use": "@nuxtjs/vercel-builder",
      "config": {
        "serverMiddleware": ["~/server.mjs"]
      }
    }
  ],
  "routes": [
    { "src": "/api/sse", "dest": "server.mjs" },
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
